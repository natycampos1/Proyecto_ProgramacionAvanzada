<!DOCTYPE html>

<html lang="es">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    ```
    <title>Sistema</title>

    <!-- Bootstrap CDN (NO archivos locales = NO errores 404) -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    ```

</head>

<body>

    ```
    <!-- ======== MENÚ SUPERIOR SIMPLE ======== -->
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
        <div class="container-fluid">

            <a class="navbar-brand" href="#">Sistema</a>

            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#menu">
                <span class="navbar-toggler-icon"></span>
            </button>

            <div class="collapse navbar-collapse" id="menu">
                <ul class="navbar-nav">

                    <li class="nav-item">
                        <a class="nav-link" href="@Url.Action("Inicio", "Home")">Ingreso</a>
                    </li>

                    <li class="nav-item">
                        <a class="nav-link" href="@Url.Action("About", "Home")">Página Principal</a>
                    </li>

                    <li class="nav-item">
                        <a class="nav-link" href="@Url.Action("Login", "Home")">Registro</a>
                    </li>

                    <li class="nav-item">
                        <a class="nav-link" href="@Url.Action("Informacion", "Home")">Información</a>
                    </li>

                    <li class="nav-item">
                        <a class="nav-link" href="@Url.Action("Horario", "Home")">Horarios y Rutas</a>
                    </li>

                </ul>
            </div>

        </div>
    </nav>

    <!-- ======== CONTENIDO ======== -->
    <div class="container mt-4">
        @RenderBody()
    </div>

    <!-- ======== FOOTER ======== -->
    <footer class="text-center mt-5 mb-3 text-muted">
        Proyecto Universitario
    </footer>

    <!-- Bootstrap JS CDN -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    ```

</body>
</html>
